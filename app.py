course = "Python Programming"
message = """
Hi John,

blah blah blah text between multiple lines
"""
print(len(course))  # len of the string
print(course[0])  # prints the characters in place of index 0
print(course[-1])  # prints the last character
print(course[0:3])  # prints from the 1st to the 4th character
print(course[0:])  # prints everything from the first letter and after
print(course[:3])  # prints everything before the 4th character inclusive
